{
  "name": "smartlearn-static",
  "version": "1.0.0",
  "private": true,
  "description": "Static build for SmartLearn frontend served from dist",
  "scripts": {
    "build": "echo 'no build: using prebuilt dist'",
    "start": "echo 'serve dist on Render static site'"
  }
}
